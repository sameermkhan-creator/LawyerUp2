<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lawyerup2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LawyerUp2">
        <activity android:name=".ticketassigned"></activity>
        <activity android:name=".aboutticket" />
        <activity android:name=".numviolations" />
        <activity android:name=".newcase" />
        <activity android:name=".ticketwhere" />
        <activity android:name=".Where" />
        <activity android:name=".signup" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_main2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>